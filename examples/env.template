# Copy this file outside of prod_module (e.g. ../bankdata.local.env) and point
# BANKDATA_ENV_FILE to the new location so the module never stores secrets.
BANKDATA_API_BASE=https://bankaccountdata.gocardless.com/api/v2
BANKDATA_ACCOUNT_ID=
BANKDATA_SECRET_ID=
BANKDATA_SECRET_KEY=
# Optional overrides
BANKDATA_STORAGE_ROOT=runtime
BANKDATA_TOKEN_CACHE=runtime/tokens/access_token.json
BANKDATA_SAMPLE_MODE=false
